#include <iostream>
#include "UPilaNum.h"

using namespace std;

int main() {
	PilaNum* p = new PilaNum();
	p->meter(1);
	p->meter(2);
	p->meter(3);
	p->meter(4);
	p->meter(5);
	p->meter(6);
    cout << p->to_str() << endl;
	system("pause");
	return 0;
}
